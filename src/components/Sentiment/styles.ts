import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  emoji: {
    fontSize: 32
  },
  sentiment: {
    padding: 10,
    color: '#CECECE',
    fontSize: 22,
    fontWeight: '700',
  },
});
